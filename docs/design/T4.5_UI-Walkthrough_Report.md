# Portfolio 组合权益模块 UI/UE 走查清单

> 对比 T3 设计稿 (Design_Portfolio.md v1.0) 与 T4/T5 前后端实现
> 走查人：Designer | 日期：2026-05-14
> 前端文件：`frontend/src/views/portfolio/PortfolioView.vue` + composables + types + api
> 后端文件：`backend/src/handlers/portfolio.rs` + `services/portfolio.rs` + `db/portfolio.rs`

---

## 走查结论

- **P0 阻塞项：8 个** — 必须修复后才能进入 T6
- **P1 重要差异：13 个** — 应在正式发布前修复
- **P2 改进项：10 个** — 可排入后续迭代

---

## 1. 总览卡片区

### 1.1 数值格式

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| C-01 | 金额千分位分隔 | `¥100,000.00` 千分位逗号 | `formatMoney` 用 `Intl.NumberFormat('en-US')` 输出 `100,000.00`，前端拼接 `¥` | ✅ 通过 | — |
| C-02 | 货币符号位置 | 金额前缀 `¥` | 累计盈亏/当日盈亏拼接 `pnlPrefix + '¥' + formatMoney`，正数显示 `+¥100` | ⚠️ **P1** | 设计稿 `+¥15,000.00`，实现 `+¥` 前缀正确，但总资产卡片无 `¥` 前缀（设计稿 `¥100,000.00` 有前缀），实现第42行 `¥{{ formatMoney(...) }}` 正确 ✅ |
| C-03 | 正数前缀 `+` | 正数前缀 `+`，零无前缀 | `pnlPrefix` 函数正确，`formatRate` 正数加 `+` | ✅ 通过 | — |
| C-04 | 小数精度 2 位 | 金额保留 2 位小数，百分比保留 2 位小数 | `formatMoney` 强制 2 位，`formatRate` 用 `toFixed(2)` | ✅ 通过 | — |
| C-05 | 零值颜色 | 零值使用 `--color-neutral` | `pnlColor` 对 `!value` 或 `num===0` 返回 `--color-neutral` | ✅ 通过 | — |
| C-06 | 累计盈亏子值百分比 | `+15.00%` 同色 | `cumulative_pnl_rate` 用 `pnlColor` 着色 | ✅ 通过 | — |
| C-07 | 当日盈亏子值百分比 | `+1.25%` 同色 | `daily_pnl_rate` 用 `pnlColor` 着色 | ✅ 通过 | — |
| C-08 | 收益率卡片 sparkline | 迷你 sparkline（40x16px，最近30天收益率趋势） | **未实现**，仅有纯文本百分比 | 🔴 **P1** | 缺少迷你图，影响数据感知 |
| C-09 | 卡片悬停动画 | translate-y -2px + border-color + box-shadow | CSS `.metric-card:hover` 实现匹配 | ✅ 通过 | — |
| C-10 | 骨架屏 | 加载中显示骨架屏 | 实现了 `skeleton-line` 动画 | ✅ 通过 | — |
| C-11 | 钱包图标透明度 | `Wallet` 图标 `--color-accent` 40% | CSS `&__icon { opacity: 0.4 }` | ✅ 通过 | — |
| C-12 | 主值字体 | JetBrains Mono 28px/700 | CSS `&__value { font-family: 'JetBrains Mono'...; font-size: 28px; font-weight: 700 }` | ✅ 通过 | — |

### 1.2 数值闪烁（WS 更新）

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| C-13 | WS 推送时数值闪烁 | 背景高亮渐隐 2s，正/负分别用绿/红色 | **未实现** — composable 有 `applyWsUpdate` 方法，但 PortfolioView.vue 中未订阅 WS，无闪烁动画 | 🔴 **P0** | WS 实时更新未接入，是核心功能缺失 |
| C-14 | WS 频道订阅生命周期 | 进入页面 subscribe 3 频道，离开 unsubscribe | **未实现** — `onMounted/onUnmounted` 无 WS 逻辑 | 🔴 **P0** | 同上 |

---

## 2. 权益曲线区

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| EC-01 | 图表类型 | ECharts 面积图 (area) | 实现 `areaStyle` 渐变填充 | ✅ 通过 | — |
| EC-02 | 渐变填充 | `--color-accent` 20% → 0% | `rgba(113,112,255,0.20)` → `rgba(113,112,255,0.00)` | ✅ 通过 | — |
| EC-03 | 线颜色/宽度 | `#7170ff` / 2px | `lineStyle: { color: '#7170ff', width: 2 }` | ✅ 通过 | — |
| EC-04 | smooth 曲线 | `smooth: 0.3` | 实现 `smooth: 0.3` | ✅ 通过 | — |
| EC-05 | Tooltip 格式 | 十字线 + 数值 `{time}<br/>权益: ¥{value}` | 实现 `trigger: 'axis'`，formatter 正确 | ✅ 通过 | — |
| EC-06 | Tooltip 背景/边框 | `#212223` / `rgba(255,255,255,0.08)` | 实现匹配 | ✅ 通过 | — |
| EC-07 | Tooltip 字体 | JetBrains Mono | `textStyle: { fontFamily: 'JetBrains Mono, monospace' }` | ✅ 通过 | — |
| EC-08 | 网格边距 | `grid: { left: 64, right: 24, top: 16, bottom: 32 }` | 实现匹配 | ✅ 通过 | — |
| EC-09 | Y 轴格式 | 千分位 + `¥` 前缀 | `formatter: (val) => '¥' + formatMoney(String(val))` | ✅ 通过 | — |
| EC-10 | 图表高度 | 280px (图表区) + 16px padding + 标题 = 容器 320px | CSS `.equity-chart { height: 280px }`，但容器设计稿要求 320px 整体 | ⚠️ **P2** | 容器无明确 320px 整体高度约束 |
| EC-11 | 空态显示 | 灰色虚线 + 「暂无权益数据」 | 实现 `chart-empty` div，仅显示图标+文字，**无虚线** | ⚠️ **P2** | 缺少虚线装饰 |
| EC-12 | 粒度切换 | 小时/日/周 el-segmented | 实现匹配，默认 `日` | ✅ 通过 | — |
| EC-13 | 日期区间选择 | el-date-picker daterange, YYYY-MM-DD, 默认最近30天 | 实现匹配 | ✅ 通过 | — |
| EC-14 | 数据点 hover 显示 | hover 时显示，半径 4px | `showSymbol: false`，未配置 `emphasis` | ⚠️ **P2** | hover 数据点未配置 emphasis 样式 |
| EC-15 | 粒度切换动画 | 图表重绘动画 300ms | **未实现** — 切换粒度后直接 `renderChart()` 全量重绘 | ⚠️ **P2** | — |
| EC-16 | WS 权益追加 | ECharts `appendData` 无全量重绘 | **未实现** — 无 WS 订阅 | 🔴 **P0** | 同 C-13 |

---

## 3. 持仓汇总表

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| PT-01 | 列定义完整 | 交易对/方向/数量/均价/现价/浮动盈亏 (6列) | 实现匹配 6 列 | ✅ 通过 | — |
| PT-02 | SideTag 多头 | `el-tag type="success"` 文字「多」 | `<el-tag :type="row.side === 'long' ? 'success' : 'danger'" size="small">{{ row.side === 'long' ? '多' : '空' }}</el-tag>` | ✅ 通过 | — |
| PT-03 | SideTag 空头 | `el-tag type="danger"` 文字「空」 | 同上 | ✅ 通过 | — |
| PT-04 | UnrealizedPnlCell 双行 | 金额在上 (14px) + 百分比在下 (12px 较浅) | `.pnl-cell { flex-direction: column }` + `.pnl-rate { font-size: 12px; opacity: 0.75 }` | ✅ 通过 | — |
| PT-05 | 多头行背景 | `--color-long-bg` (10%) | `:deep(.row-long) { background-color: rgba(103,194,58,0.04) }` | ⚠️ **P1** | 设计稿要求 10% 透明度（0.10），实现为 0.04，颜色偏淡 |
| PT-06 | 空头行背景 | `--color-short-bg` (10%) | `:deep(.row-short) { background-color: rgba(245,108,108,0.04) }` | ⚠️ **P1** | 同上，0.04 vs 设计 0.10 |
| PT-07 | 行悬停 | 行背景 `rgba(255,255,255,0.04)` | 依赖 el-table 默认 hover，未自定义 | ⚠️ **P1** | 可能与暗色主题不匹配 |
| PT-08 | 默认排序 | 按浮动盈亏绝对值降序 | `:default-sort="{ prop: 'unrealized_pnl', order: 'descending' }"` | ⚠️ **P1** | 设计要求"绝对值降序"，el-table 默认排序非绝对值 |
| PT-09 | 交易对列头可排序 | 可点击列头排序 | 仅 `unrealized_pnl` 列加了 `sortable`，**交易对列未加** | ⚠️ **P1** | 设计稿要求交易对字母序可排 |
| PT-10 | 分页 | el-pagination small, 每页 10 条 | 实现匹配 | ✅ 通过 | — |
| PT-11 | 持仓数量 Badge | 标题行右侧 Badge | `<el-badge v-if="summary?.total_positions" :value="summary.total_positions" type="info" />` | ✅ 通过 | — |
| PT-12 | 空态 | 「暂无持仓」图标+文字 | `empty-text="暂无持仓"` | ⚠️ **P2** | 仅 el-table 默认空文字，缺少 EmptyPortfolio 组件（图标+引导文案+跳转按钮） |
| PT-13 | 筛选栏 | 方向筛选 + 交易对搜索 + 排序方式 | **未实现** — 无筛选栏，composable 有 filters 但 PortfolioView 中未使用 | 🔴 **P0** | 持仓筛选/搜索功能完全缺失 |
| PT-14 | WS 行闪烁 | 行背景闪烁对应方向色 2s | **未实现** | 🔴 **P0** | 同 C-13 |
| PT-15 | 等宽字体 | 数量/均价/现价/盈亏使用等宽字体 | `.mono-text { font-family: 'JetBrains Mono'... }` | ✅ 通过 | — |

---

## 4. 绩效指标区

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| PM-01 | MaxDD 颜色 | 始终 `--color-negative` | CSS `.color-negative` | ✅ 通过 | — |
| PM-02 | MaxDD 迷你图 | 小型面积图（40x20px），回撤区间红色填充 | **未实现** | ⚠️ **P1** | 缺少迷你回撤图 |
| PM-03 | MaxDD 图标 | `Bottom` 图标 16px | **未实现** | ⚠️ **P2** | — |
| PM-04 | Sharpe 颜色阈值 | ≥2 positive, 1-2 warning, <1 negative | `sharpeColor` computed 实现匹配 | ✅ 通过 | — |
| PM-05 | Sharpe 图标 | `DataAnalysis` 图标，颜色跟随 | **未实现** | ⚠️ **P2** | — |
| PM-06 | WinRate 颜色阈值 | ≥60% positive, 40-60% warning, <40% negative | `winRateColor` computed 实现匹配 | ✅ 通过 | — |
| PM-07 | WinRate 半圆进度环 | 半圆进度环 (36x20px) | **未实现** | ⚠️ **P1** | 缺少进度环 |
| PM-08 | WinRate 图标 | `SuccessFilled` 图标 | **未实现** | ⚠️ **P2** | — |
| PM-09 | 绩效卡片容器 | `--color-surface`, border-radius 12px, padding 16px | `.perf-metric-card` 实现匹配 | ✅ 通过 | — |
| PM-10 | 绩效数值字体 | 22px/700, JetBrains Mono | CSS 实现匹配 | ✅ 通过 | — |

---

## 5. 策略绩效对比表

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| ST-01 | 列定义 | 策略名称/总盈亏/盈亏率/最大回撤/交易次数/胜率 (6列) | 实现匹配 6 列 | ✅ 通过 | — |
| ST-02 | 策略名称可点击 | 链接色，点击跳转策略详情 | CSS `.strategy-link` 有 hover underline，**但无 click 事件/路由跳转** | ⚠️ **P1** | 仅有视觉，无功能 |
| ST-03 | 总盈亏颜色 | 正负颜色跟随 | `pnlColor(row.total_pnl)` | ✅ 通过 | — |
| ST-04 | 盈亏率颜色 | 正负颜色跟随 | `pnlColor(row.total_pnl_rate)` | ✅ 通过 | — |
| ST-05 | 最大回撤颜色 | 始终 `--color-negative` | `.color-negative` | ✅ 通过 | — |
| ST-06 | 胜率颜色阈值 | ≥60% positive, 40-60% warning, <40% negative | `winRateColorFor(row.win_rate)` 实现匹配 | ✅ 通过 | — |
| ST-07 | 默认排序 | 按总盈亏降序 | `:default-sort="{ prop: 'total_pnl', order: 'descending' }"` | ✅ 通过 | — |
| ST-08 | 列头可排序 | 总盈亏/盈亏率可排序 | 仅 `total_pnl` 和 `total_pnl_rate` 加了 `sortable`，最大回撤/交易次数/胜率未加 | ⚠️ **P2** | — |
| ST-09 | 空态 | 「暂无策略数据」Empty 组件 | `empty-text="暂无策略数据"` | ⚠️ **P2** | 仅文字，缺图标+引导 |
| ST-10 | 迷你 sparkline | 策略行含迷你 sparkline | **未实现** | ⚠️ **P2** | — |

---

## 6. 路由与页面结构

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| RT-01 | 3 个路由 | `/portfolio` + `/portfolio/positions` + `/portfolio/performance` | 仅实现 `/portfolio` 一个路由，**后两个子页面未实现** | 🔴 **P0** | 缺少独立持仓汇总页和策略绩效对比页 |
| RT-02 | 侧边栏导航 | 侧边栏「组合权益」菜单项，图标 `PieChart` | 路由 meta `{ title: '持仓' }`，**标题与设计稿不一致** | ⚠️ **P1** | 设计稿「组合权益」，实现「持仓」 |
| RT-03 | 页面2 多策略叠加图 | 策略绩效对比页含多策略权益曲线叠加图 (ECharts multi-line) | **未实现** — 无独立绩效页 | 🔴 **P0** | 同 RT-01 |
| RT-04 | 页面2 持仓占比列 | 持仓汇总详细页含「占比」列 + 迷你进度条 | **未实现** | 🔴 **P0** | 同 RT-01 |

---

## 7. 响应式适配

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| RS-01 | 桌面端 (≥1200px) | 4列卡片 + 60/40 分栏 | 实现匹配：`el-col :lg="6"` + `.positions-section { flex: 6 }` / `.performance-section { flex: 4 }` | ✅ 通过 | — |
| RS-02 | 平板端 (768-1199px) | 2x2 卡片 + 底部单栏 + 隐藏均价列 | `el-col :sm="12"` 实现 2x2；`@media (max-width: 1199px)` 底部变单栏；**但未隐藏均价列** | ⚠️ **P1** | 设计稿要求平板端隐藏均价列 |
| RS-03 | 移动端 (<768px) | 单列卡片 + 表格改卡片列表 + 曲线 240px + 筛选折叠 | `@media (max-width: 767px)` 仅做了 header 列化和绩效卡纵向堆叠；**表格未改为卡片列表模式，曲线高度未缩减，筛选栏未折叠** | 🔴 **P0** | 移动端适配严重不足 |
| RS-04 | 移动端权益曲线高度 | 240px | 实现 280px 固定，无响应式调整 | ⚠️ **P1** | — |

---

## 8. 空状态与错误处理

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| EE-01 | 新用户空组合 | EmptyPortfolio 组件：图标+文案+「前往交易」按钮 | **未实现** — 无 EmptyPortfolio 组件 | 🔴 **P1** | 仅 el-table 默认空文字 |
| EE-02 | 无持仓空态 | EmptyPosition: Wallet 图标 + 「当前没有持仓」 | `empty-text="暂无持仓"`，无图标引导 | ⚠️ **P1** | — |
| EE-03 | API 401/403/500 | 分级错误处理：401 跳登录、403 result 页、500 重试 | 仅 `el-alert` 统一显示错误，**无分级处理** | 🔴 **P0** | 安全关键：401 应跳登录 |
| EE-04 | WS 断开横幅 | 顶部「实时数据连接断开，正在重连...」 | **未实现** — 无 WS 逻辑 | 🔴 **P0** | 同 C-13 |
| EE-05 | 加载超时 | 超过 10s → 「加载超时」 + 「重新加载」 | **未实现** — 无超时机制 | ⚠️ **P1** | — |

---

## 9. 后端 API 对齐

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| BA-01 | GET /summary 响应字段 | total_equity/daily_pnl/daily_pnl_rate/cumulative_pnl/cumulative_pnl_rate/total_positions/updated_at | 后端字段匹配，前端类型匹配 | ✅ 通过 | — |
| BA-02 | GET /positions 分页 | page/size/symbol/side 参数 | 后端支持，但默认 `size=20`（前端 composable 默认 10） | ⚠️ **P1** | 前端传 `size=10`，后端默认 20，若前端未传参会不一致 |
| BA-03 | GET /performance 缺少整体指标 | 设计稿要求整体 max_drawdown/sharpe_ratio/win_rate | 后端 `PortfolioPerformance` **仅含 `strategies` 数组**，缺少整体 `max_drawdown/sharpe_ratio/win_rate` | 🔴 **P0** | 前端类型定义有这 3 个字段，但后端不返回，导致绩效卡片显示 `--` |
| BA-04 | GET /equity_curve 粒度 | 小时/日/周 | 后端支持，但 `week` 粒度逻辑与 `day` 相同（仅 hour 和 default 两个分支） | ⚠️ **P1** | week 粒度未实现周级别聚合 |
| BA-05 | Positions side 筛选 | 支持 `side` 参数 | 后端 `PortfolioPositionsQuery` 无 `side` 字段，**不支持方向筛选** | 🔴 **P0** | 前端传 `side=long/short` 后端会忽略 |
| BA-06 | 数值精度 string | B4 契约：Decimal → string | 后端 `fmt_money`/`fmt_pct` 输出 string，前端类型为 string | ✅ 通过 | — |
| BA-07 | 金额千分位 | 后端返回 string "100000.00" 无千分位 | 前端 `formatMoney` 添加千分位 | ✅ 通过 | 千分位由前端负责，正确 |
| BA-08 | current_price 占位 | 真实系统应从 market_data 获取 | 后端注释 `Use avg_entry_price as current_price placeholder` | ⚠️ **P1** | 现价=均价导致浮动盈亏恒为 unrealized_pnl 字段值，数据不准确 |
| BA-09 | 权益历史表 | portfolio_equity_history 存 equity snapshot | DB Entity 已实现 + `record_equity_snapshot` 服务 | ✅ 通过 | — |

---

## 10. 交互细节与动画

| # | 检查项 | 设计规格 | 实现 | 严重度 | 备注 |
|---|--------|---------|------|--------|------|
| IA-01 | 刷新按钮旋转 | 点击旋转 360deg, 0.6s | `@keyframes spin-once` + `.refresh-spinning :deep(.el-icon)` | ✅ 通过 | — |
| IA-02 | 骨架屏动画 | shimmer 1.5s infinite | `@keyframes skeleton-shimmer` | ✅ 通过 | — |
| IA-03 | 数值闪烁动画 | 正/负分别绿/红背景高亮 2s | **未实现** | 🔴 **P0** | 同 C-13 |
| IA-04 | 行闪烁动画 | WS 更新行闪烁方向色 2s | **未实现** | 🔴 **P0** | 同 C-13 |
| IA-05 | 图表重绘过渡 | 300ms 过渡动画 | **未实现** | ⚠️ **P2** | — |

---

## P0 汇总（8 项，必须修复）

| ID | 差异描述 | 影响范围 |
|----|---------|---------|
| P0-1 | C-13/C-14: WS 实时更新未接入（3 个频道未订阅，无闪烁动画） | 全页面实时性 |
| P0-2 | EC-16: 权益曲线 WS 追加未实现 | 权益曲线区 |
| P0-3 | PT-13: 持仓筛选/搜索功能完全缺失（方向筛选、交易对搜索、排序方式） | 持仓汇总表 |
| P0-4 | PT-14: 持仓行 WS 闪烁未实现 | 持仓汇总表 |
| P0-5 | RT-01/03/04: 缺少 /portfolio/positions 和 /portfolio/performance 两个子页面 | 路由/导航 |
| P0-6 | RS-03: 移动端适配严重不足（表格未改卡片列表、曲线未缩、筛选未折叠） | 响应式 |
| P0-7 | EE-03: API 错误分级处理缺失（401 应跳登录，无 403/500 分级） | 错误处理 |
| P0-8 | BA-03: 后端 /performance 缺少整体 max_drawdown/sharpe_ratio/win_rate 字段 | 绩效卡片显示 |
| P0-9 | BA-05: 后端 /positions 不支持 side 筛选参数 | 持仓筛选 |

> 注：P0-1/P0-2/P0-4 本质上是同一个问题（WS 未接入），可合并为 1 项修复任务。

## P1 汇总（13 项，应在发布前修复）

| ID | 差异描述 |
|----|---------|
| P1-1 | C-08: 收益率卡片缺少迷你 sparkline |
| P1-2 | PT-05/06: 多头/空头行背景透明度 0.04 vs 设计 0.10 |
| P1-3 | PT-07: 持仓行悬停样式未自定义 |
| P1-4 | PT-08: 默认排序非绝对值降序 |
| P1-5 | PT-09: 交易对列头不可排序 |
| P1-6 | PM-02: MaxDrawdown 缺少迷你回撤面积图 |
| P1-7 | PM-07: WinRate 缺少半圆进度环 |
| P1-8 | ST-02: 策略名称仅视觉可点击，无路由跳转 |
| P1-9 | RT-02: 侧边栏标题「持仓」vs 设计「组合权益」 |
| P1-10 | RS-02: 平板端未隐藏均价列 |
| P1-11 | RS-04: 移动端权益曲线高度未缩减至 240px |
| P1-12 | EE-05: 无加载超时机制 |
| P1-13 | BA-02/04/08: 后端分页默认值不一致、week 粒度未实现、current_price 占位数据 |

## P2 汇总（10 项，可排入后续迭代）

| ID | 差异描述 |
|----|---------|
| P2-1 | EC-10: 权益曲线容器无 320px 整体高度约束 |
| P2-2 | EC-11: 空态缺少虚线装饰 |
| P2-3 | EC-14: 数据点 hover 未配置 emphasis 样式 |
| P2-4 | EC-15: 粒度切换无 300ms 过渡动画 |
| P2-5 | PT-12: 持仓空态缺 EmptyPortfolio 组件 |
| P2-6 | PM-03/05/08: 绩效卡片图标未实现 |
| P2-7 | ST-08/10: 部分列头不可排序、策略行缺 sparkline |
| P2-8 | ST-09: 策略空态仅文字，缺图标引导 |
| P2-9 | IA-05: 图表重绘无过渡动画 |
| P2-10 | EE-01/02: 空状态组件缺图标+引导文案 |

---

## 修复优先级建议

1. **WS 实时更新接入**（合并 P0-1/P0-2/P0-4） — 核心功能缺失
2. **后端 /performance 补充整体指标**（P0-8） — 绩效卡片显示空白
3. **后端 /positions 支持 side 筛选**（P0-9） — 前端筛选功能依赖
4. **持仓筛选栏前端实现**（P0-3） — 依赖 P0-9 后端先修
5. **API 错误分级处理**（P0-7） — 安全关键
6. **两个子页面实现**（P0-5） — 工作量最大，可拆分
7. **移动端适配**（P0-6） — 可拆分多轮
