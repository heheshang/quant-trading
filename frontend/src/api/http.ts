/**
 * Re-exports the HTTP client for backward compatibility.
 * New code should import from '@/api/client' directly.
 */
export { default as http } from './client'
export { default as default } from './client'
