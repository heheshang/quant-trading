# Build configuration for faster compilation

[build]
jobs = 4
incremental = true

[profile.dev]
opt-level = 0
debug = false
split-debuginfo = "unpacked"
codegen-units = 16
lto = false

[profile.test]
opt-level = 0
codegen-units = 16

[profile.release]
